from setuptools import setup

setup(name='rnn',
      version='0.1',
      description='Implementations of various RNN architectures.',
      url='https://github.com/axiom-of-joy/rnn',
      author='Xander Song',
      license='MIT',
      packages=['rnn'],
      zip_safe=False)
